---
phase: 01-us-kr-poc
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/phases/01-us-kr-poc/01-VALIDATION.md
  - .planning/phases/01-us-kr-poc/01-PR-REVIEW.md
  - docs/research/2026-09-19-kb-market-data-poc-validation.md
  - CHANGELOG.md
  - kb-us-stock-trading-journal/docs/brokers/kb/2026-09-19-market-snapshot-poc.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02, SHIP-01, SAFE-01, SAFE-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-07: Both implementations have RED/GREEN evidence, independent spec and quality review, focused regression checks and an OpenCharts production build."
    - "VERIFY-02: A desktop and 390px browser verify real input → proxy → backend → normalized DTO → chart for both market fixtures and live markets where credentials and entitlement permit."
    - "E-01..E-07, P-01..P-03 and UI-01..UI-05 are traced to concrete evidence; unavailable live evidence is a named limitation rather than a fake success."
    - "D-08: Original dirty worktrees/processes are preserved; OpenCharts PR targets milestone/kb-market-data-poc, KB PR targets dev, and both remain open."
    - "D-07/SHIP-01: Every remediation loop publishes original findings before changes and per-finding commit/test dispositions after changes. Both final current PR HEADs have fresh P0=0/P1=0 review, reported P2 and observed required checks."
  prohibitions:
    - statement: "P-01: No order TR, arbitrary provider path, credential leak, raw payload in public response"
      status: resolved
      verification: explicit
    - statement: "P-02: No synthetic quote/volume or crypto replay fallback in connected mode"
      status: resolved
      verification: explicit
    - statement: "P-03: No relabelling an unverified Cboe instrument as Nasdaq"
      status: resolved
      verification: explicit
  artifacts:
    - path: .planning/phases/01-us-kr-poc/01-VALIDATION.md
      provides: "Evidence index separating fixture, browser, live and CI results"
    - path: docs/research/2026-09-19-kb-market-data-poc-validation.md
      provides: "Sanitized reviewable public validation and limitations"
    - path: .planning/phases/01-us-kr-poc/01-PR-REVIEW.md
      provides: "Two PR URLs, final HEADs, finding dispositions and required-check evidence"
    - path: kb-us-stock-trading-journal/docs/brokers/kb/2026-09-19-market-snapshot-poc.md
      provides: "Backend-owned actual US/KR bounded live acceptance or blocker"
  key_links:
    - from: .planning/phases/01-us-kr-poc/01-VALIDATION.md
      to: e2e/market-lookup.spec.ts
      via: "Exact tested commit and command for both-market real application wiring"
    - from: .planning/phases/01-us-kr-poc/01-PR-REVIEW.md
      to: docs/research/2026-09-19-kb-market-data-poc-validation.md
      via: "PR bodies cite current validation and live limitations"
    - from: .planning/phases/01-us-kr-poc/01-PR-REVIEW.md
      to: kb-us-stock-trading-journal/docs/brokers/kb/2026-09-19-market-snapshot-poc.md
      via: "Backend market-specific evidence with history/ingest gates unchanged"
---

<objective>
연결 POC의 실제 검증 범위와 제한을 확인하고 두 저장소의 검토된 열린 PR을 전달한다. D-07/D-08대로 실행·리뷰·수정·CI를 마치며 병합하지 않는다. 기존 US/KR fixture 테스트와 실제 KB 호출 결과는 별개의 증거다.
</objective>

<execution_context>
executing-plans subagents mode + subagent-driven-development + test-driven-development를 사용한다. 계획/검토 gpt-6-astra, 구현/수정 gpt-5.6-sol 지정은 유지한다. Task 1 종료 시 독립 spec review → 독립 quality review를 마친 후 Task 2로 진행한다. 조건부 corrective code changes도 RED/GREEN과 해당 두 리뷰를 거친다. 사용자가 이미 PR 생성·findings/disposition comment 게시를 승인했으므로 일반 mode/PR-choice 승인을 다시 묻지 않는다.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-us-kr-poc/01-CONTEXT.md
@.planning/phases/01-us-kr-poc/01-SPEC.md
@.planning/phases/01-us-kr-poc/01-UI-SPEC.md
@.planning/phases/01-us-kr-poc/OFFICIAL-CONTRACT.md
@.planning/phases/01-us-kr-poc/01-01-PLAN.md
@.planning/phases/01-us-kr-poc/01-01-SUMMARY.md
@.planning/phases/01-us-kr-poc/01-02-PLAN.md
@.planning/phases/01-us-kr-poc/01-02-SUMMARY.md
@.planning/phases/01-us-kr-poc/SOURCE-AUDIT.md
</context>

<repository_map>
OC/OpenCharts and KB/kb-us-stock-trading-journal retain the two distinct workdirs assigned by the orchestrator in 01-01. kb-us-stock-trading-journal/ is an external-owner prefix, not a directory under OC. Execute each command in the named repository, with paths relative to that repository. Do not change another worktree's branch or stop its process. Runtime port/workdir values remain local; public prose uses configuration names only. Shared backend runtime/credentials, browser server and build output have one verification owner and are not tested concurrently by independent workers.
</repository_map>

## Artifacts this phase produces

| Repository | Artifact | New or existing |
|---|---|---|
| OC | .planning/phases/01-us-kr-poc/01-VALIDATION.md | NEW evidence index, including exact commands/SHAs and E/P/UI mapping |
| OC | docs/research/2026-09-19-kb-market-data-poc-validation.md | NEW sanitized public result; no private coordinates, aliases or IDs |
| OC | .planning/phases/01-us-kr-poc/01-PR-REVIEW.md | NEW PR/HEAD/review/check record |
| OC | CHANGELOG.md | Update existing file, or create if absent, for new connected-entry/dev-mode workflow |
| KB | docs/brokers/kb/2026-09-19-market-snapshot-poc.md | Extend 01-01 Task 1/2 actual market-specific evidence; never overwrite an adverse result |

<tasks>
<task type="auto">
  <name>Task 1: US/KR 브라우저 경로와 제한된 실제 계정 검증을 증거로 남긴다</name>
  <reversibility rating="reversible">추가 검증 기록과 기존 승인 범위의 제한된 읽기이며 계정 데이터나 원본 프로세스를 변경하지 않는다.</reversibility>
  <files>OC: .planning/phases/01-us-kr-poc/01-VALIDATION.md, docs/research/2026-09-19-kb-market-data-poc-validation.md, CHANGELOG.md. KB: docs/brokers/kb/2026-09-19-market-snapshot-poc.md</files>
  <read_first>01-01-SUMMARY.md and 01-02-SUMMARY.md and their exact tested SHAs; SPEC/UI tables; OFFICIAL-CONTRACT.md bounded-live constraints; both AGENTS.md; backend-owned contract/POC records; current browser/verification skill guidance. Existing passing commands remain reusable unless relevant code/config/dependencies changed.</read_first>
  <action>
    Verify 01-01 and 01-02 task reviews and compare tested SHAs with current changes. Reuse unchanged focused passing evidence, then run the real HTTP Playwright suite and perform one visual desktop/mobile pass with the current installed browser tooling. Check both editable symbols, safe account alias selection, US/KR query-market labels, Enter/button submission, delayed/unknown labels, unavailable times/volume, submitted-versus-edited identity, rapid US/KR/account switching and a provider/backend failure. Inspect actual candles and volume omission, keyboard focus/error announcement, 390px no-overflow, resize/unmount cleanup, and navigation to/from the preserved demo via full document loads. Verify no connected-page demo feed, paper order requests or telemetry initialization, with the configured test-key spy and the browser network log. An HTTP 200 or server startup alone is not visual validation.

    Reuse accepted bounded live US/KR observations from 01-01 if the final code and contract are unchanged. If either approved probe remains unperformed and the selected account's existing binding/token ownership permits it, perform at most one quote and one daily read for MU/NAS and one quote/one daily for 005930/KOSPI, serially, through the actual connected browser/backend route. Use existing backend credential binding only; do not print or export credentials, tokens, raw account/connection identifiers, raw responses or private coordinates. Never change credential binding/quarantine, bypass ownership, kill original processes, enumerate holdings/history, submit orders, or repeat a refused operation automatically. Provider responses must pass the production validators. Confirm KR inq_clsf=1 with explicit current Seoul date actually supplies the intended recent series, echoed market/symbol and accepted business status before its production activation. KOSDAQ/DRAM live checks are conditional on a separately verified supported route and are not a reason to broaden the request budget or invent mapping. Preserve execution-history/ingest gates regardless of snapshot success.

    Capture only public instrument/query market, counts, currency, observed delay classification, acquisition/check time, accepted status and sanitized success/blocker. Use the backend result record for market activation status. Distinguish fixture wiring, actual visual pass, live provider acceptance, unsupported entitlement/market-hours limitations and CI. A blocked live prerequisite is reportable completion of the attempted verification task, not proof of live success; keep that market fail-closed and explicitly list what must change for acceptance. If browser automation cannot run, report the missing capability and leave VERIFY-02 pending rather than substituting screenshots from fixtures or treating a process check as sufficient.

    Write the OC public validation report and private planning evidence index without copying local configuration. Include an evidence row for every E-01..E-07/P-01..P-03/UI-01..UI-05 and all twelve requirements. Update CHANGELOG for the connected entry and kb development mode. Run relevant Ruff/type checks if not already recorded for final backend code; verify existing generated contracts match the exporter. Treat a corrective backend change as owned 01-01 scope and a corrective frontend/fixture-server change as owned 01-02 scope, add a regression that fails first, then obtain independent spec and quality reviews before proceeding. Do not rerun unrelated baseline suites or rebuild unchanged output merely to make a new timestamp.
  </action>
  <acceptance_criteria>Both-market real application wiring and one desktop/390px visual pass have actual evidence or explicit unavailable status; every live market is independently accepted or named blocked, with no secret/raw payload artifacts. Provider-contract/date-direction uncertainty is resolved before activation. All SPEC/UI rows and test/build results link to exact final tested SHAs. Original worktrees/processes remain preserved. Independent spec and quality review findings are addressed.</acceptance_criteria>
  <verify>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm exec -- playwright test --config playwright.kb.config.ts</automated>
    <fails_when>Playwright exits nonzero, reports no tests, fails an owned server launch, times out, or fails a required US/KR/390px/error/race case. Required skipped cases leave verification incomplete.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>git diff --check</automated>
    <fails_when>Git exits nonzero and reports whitespace errors or conflict markers in the current OC diff.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>git diff --check</automated>
    <fails_when>Git exits nonzero and reports whitespace errors or conflict markers in the current KB diff.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>uv run --frozen --no-sync ruff check src/kb_journal/api/market_snapshot.py src/kb_journal/api/router.py src/kb_journal/broker/contracts.py src/kb_journal/broker/adapters/kb/adapter.py src/kb_journal/broker_sync/adapters/kb/adapter.py src/kb_journal/broker_sync/coordinator.py tests/broker/test_kb_adapter.py tests/broker_sync/test_coordinator.py tests/api/test_market_snapshot.py tests/market_snapshot_fixture_server.py</automated>
    <fails_when>Ruff exits nonzero with rule violations or invalid paths/configuration in the changed Python files.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>uv run --frozen --no-sync ruff format --check src/kb_journal/api/market_snapshot.py src/kb_journal/api/router.py src/kb_journal/broker/contracts.py src/kb_journal/broker/adapters/kb/adapter.py src/kb_journal/broker_sync/adapters/kb/adapter.py src/kb_journal/broker_sync/coordinator.py tests/broker/test_kb_adapter.py tests/broker_sync/test_coordinator.py tests/api/test_market_snapshot.py tests/market_snapshot_fixture_server.py</automated>
    <fails_when>Ruff exits nonzero or reports files that would be reformatted.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>uv run --frozen --no-sync mypy src</automated>
    <fails_when>mypy exits nonzero or reports type errors; record any pre-existing error separately rather than treating the entire command as passing.</fails_when>
    <human-check>Agent-operated browser visual inspection at desktop and 390px, plus bounded live broker observation only when prerequisites permit. Record actual run or precise unavailable reason; an unavailable browser leaves VERIFY-02 pending. Reuse 01-01 focused pytest and 01-02 unit/build evidence only when their code/config/dependencies are unchanged. Inspect P-01/P-02/P-03 negative-case results, not just command exit status.</human-check>
  </verify>
  <done>Reviewable sanitized validation distinguishes implemented/fixture-verified/live-accepted/blocked outcomes, and the user can identify the exact operational boundary for each market.</done>
</task>

<task type="auto">
  <name>Task 2: 두 PR을 열고 현재 HEAD의 P0/P1와 required checks를 확인한다</name>
  <reversibility rating="reversible">검토 가능한 열린 PR과 comment만 게시하며 merge와 branch 삭제는 수행하지 않는다.</reversibility>
  <files>OC: .planning/phases/01-us-kr-poc/01-PR-REVIEW.md</files>
  <read_first>01-VALIDATION.md; actual branch/status/diff and remote configuration of both repositories; both task review outcomes; existing repository PR templates and any configured automatic review integration. Use receiving-code-review for findings and verification-before-completion for final claims.</read_first>
  <action>
    Per D-07/D-08 inspect both dirty trees, current branch/base and complete proposed PR ranges. Stage only this task's authorized repository changes; preserve unrelated staged/unstaged files. Verify OC phase branch derives from milestone/kb-market-data-poc and KB feat/opencharts-market-data-poc targets dev. Never switch or overwrite the original dirty roots. Commit scoped changes with the recorded RED/GREEN/review evidence, prepare concrete concise PR bodies naming the new behavior, tests/build, same-account read-only boundary, both-market live limitations and companion repository relationship. Scrub artifacts, screenshots and bodies for secrets/private aliases/identifiers/coordinates; do not upload raw tool logs or provider bodies. Complete internal task review/fixes before opening when an automatic external review integration exists.

    Push and open one PR in kimsama/OpenCharts with base milestone/kb-market-data-poc and one in kimsama/kb-us-stock-trading-journal with base dev. Use repository templates if present. Multiline gh bodies/comments use exact UTF-8 temporary body files with --body-file, never shell-interpolated strings. Include cross-links once both URLs exist. If missing CLI authentication or a remote policy truly blocks creation, record the concrete rejected action and prerequisite; do not manufacture URLs or claim delivery.

    Independently review each actual PR change range at its current remote HEAD using gpt-6-astra. For every loop publish original findings with severity/reachable scenario/file:line before any worker modifies code; then assign gpt-5.6-sol the explicit affected repository/files, preserving others' edits. After RED/GREEN fixes and affected checks, publish a separate per-finding disposition with what changed, commit and test evidence. Re-review the new current HEAD and report P0/P1/P2 counts every iteration. Continue until P0=0 and P1=0 for both current final heads; list remaining P2 without adding a P2=0 requirement. Review the corrected ranges and affected behavior rather than unrelated code, while final acceptance must refer to the complete current PR range and SHA.

    Observe repository required checks on those exact final reviewed remote SHAs. Wait for pending checks with short bounded polling; classify failures and repair task-related issues through the same loop. If the repository has no required checks, record that distinction and any available run statuses; a local pass or successful push is not CI evidence. Confirm headRefOid still equals each final reviewed SHA after checks. Record PR URLs, bases, exact HEADs, independent verdicts, all dispositions, remaining P2 and required-check evidence in 01-PR-REVIEW.md. Stop with both PRs OPEN. Never merge, delete branches/worktrees or claim dev integration in this scope.
  </action>
  <acceptance_criteria>Two real open PRs target the approved distinct bases; complete current ranges were reviewed independently; both final remote HEADs have P0=0/P1=0, explicit remaining P2 and successful required checks where configured. Every remediation has original findings before changes and per-finding commit/test disposition after. PR descriptions contain honest live limitations and companion links. No integration/cleanup occurred.</acceptance_criteria>
  <verify>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>gh pr view --json url,state,baseRefName,headRefOid,reviewDecision,statusCheckRollup</automated>
    <fails_when>gh exits nonzero or returns no PR JSON. This is retrieval only; OPEN/base/SHA acceptance is the explicit mandatory human-check below.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>gh pr checks --required</automated>
    <fails_when>gh exits nonzero, reports pending/failed configured required checks, or returns a no-required-checks result that has not been confirmed against actual repository policy. No checks is a distinct recorded policy state, not an automatic pass.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>gh pr view --json url,state,baseRefName,headRefOid,reviewDecision,statusCheckRollup</automated>
    <fails_when>gh exits nonzero or returns no PR JSON. This is retrieval only; OPEN/base/SHA acceptance is the explicit mandatory human-check below.</fails_when>
    <cwd repository="KB">kb-us-stock-trading-journal</cwd>
    <automated>gh pr checks --required</automated>
    <fails_when>gh exits nonzero, reports pending/failed configured required checks, or returns a no-required-checks result that has not been confirmed against actual repository policy. No checks is a distinct recorded policy state, not an automatic pass.</fails_when>
    <human-check>MANDATORY agent-executed value comparison, recorded in 01-PR-REVIEW.md: both returned states equal OPEN; OC baseRefName equals milestone/kb-market-data-poc; KB baseRefName equals dev; each headRefOid equals the exact SHA independently reviewed last; configured required checks belong to and pass at that SHA; each fresh review has P0=0 and P1=0 with P2 reported. Any mismatch blocks SHIP-01 and triggers correction/re-review. Confirm actual no-required-checks policy before marking that case not applicable. Check per-loop original findings precede fixes and separate dispositions cite commit/tests. This is evidence inspection, not a request for new user approval.</human-check>
  </verify>
  <done>The user receives both open reviewed PR links, exact final HEADs, P0/P1/P2 counts, required-check state and concrete live limitations. No merge is performed.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; high/critical findings block delivery.

| Boundary | Description |
|---|---|
| Live backend binding → provider | Existing owner/lease and fixed read budget only, no token recovery bypass |
| Local validation → public PR/comment | Sanitized evidence only; actual account data/credentials remain local |
| Reviewed HEAD → remote PR/CI | Current reviewed commit identity must match observed checks |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08 | Information Disclosure | POC/report/PR body | high | mitigate | Allowlisted normalized outcome fields, sanitized screenshots, no raw logs/provider bodies/account identifiers/coordinates |
| T-01-09 | Elevation of Privilege | bounded live run | high | mitigate | Same-account retained ownership, no original-process termination or credential changes, closed history/order/ingest gates |
| T-01-10 | Tampering | review/check evidence | high | mitigate | Read actual remote HEAD per loop; publish original findings before fixes; dispositions and final check results tied to exact SHA |
| T-01-11 | Repudiation | reported delivery state | medium | mitigate | Distinct fixture/browser/live/CI outcomes, OPEN PR state and named unverified prerequisites |
</threat_model>

<verification>
Backend paths referenced by commands were created in 01-01; frontend/test-server paths were created in 01-02; KB commands run only in KB. OC npm scripts/package and installed Playwright, KB uv/Ruff/mypy, exporter and frontend api:generate script were grounded during planning. The external repo prefix is resolved by dispatch, never by assuming a sibling directory layout. Do not add documentation semantic-regex or hash tests. Generated contract checks and actual behavior are executable gates; prose is reviewed directly.
All implementation corrective changes rerun only affected tests and build output; complete CI is observed remotely. Task 2 cannot claim success while final required checks or P0/P1 are unresolved. A live limitation allowed by VERIFY-02 is prominently carried into both PRs and final delivery; it does not open an unverified provider runtime.
P-01 maps to actual unsafe-request/secret-sentinel backend and proxy results; P-02 to null/no-data and connected-demo-isolation results; P-03 to DRAM/query-market rejection results. The canonical prohibition projection remains resolved/explicit, but completion requires those actual negative-case results and direct independent review. Missing enforcement evidence is unverified.
</verification>

<success_criteria>
VERIFY-02 has honest current browser/live evidence and named limits. SHIP-01 has two open PRs with current independent P0/P1-free review and observed required checks, remaining P2 reported. No merge or original workspace/process disruption occurs.
</success_criteria>

<output>
Create .planning/phases/01-us-kr-poc/01-03-SUMMARY.md with two PR URLs, final reviewed HEADs, severity counts/check states, live outcomes and unfulfilled prerequisites. Update requirement status only from the actual evidence. Return the two open PRs without invoking merge/cleanup.
</output>
