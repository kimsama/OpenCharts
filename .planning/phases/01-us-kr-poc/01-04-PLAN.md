---
phase: 01-us-kr-poc
plan: "04"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/services/runtimeMode.ts
  - src/main.tsx
  - src/App.tsx
  - src/services/api.ts
  - src/services/ws.ts
  - src/services/store.tsx
  - src/services/queries.ts
  - src/lib/posthog.ts
  - src/pages/TradingPage.tsx
  - src/__tests__/runtime-mode.test.ts
  - src/__tests__/native-bootstrap.test.tsx
autonomous: true
requirements: [SAFE-01, SAFE-02, VERIFY-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-05/UI-06: KB startup renders the actual App and TradingPage shell; demo startup retains the original behavior."
    - "D-03/D-06/E-07/E-08: KB startup makes zero demo login/feed/telemetry/trading/portfolio/depth calls and cannot hydrate stale demo account/user/tick state."
    - "D-03/UI-07: Every broker/paper/account-trading mutation fails at the shared API boundary, including hidden/context/mobile call paths."
    - "Native local chart preferences/templates are still browser-local; mode isolation does not erase saved demo preferences or invent an authenticated KB user."
  prohibitions:
    - statement: "P-01: No order TR, arbitrary provider path, credential leak, raw payload in public response"
      status: resolved
      verification: explicit
    - statement: "P-02: No synthetic quote/volume, demo portfolio/depth or crypto replay fallback in KB mode"
      status: resolved
      verification: explicit
    - statement: "P-03: No relabelling an unverified Cboe instrument as Nasdaq"
      status: resolved
      verification: explicit
    - statement: "P-04: No separate connected HTML/page/chart product replacing the original OpenCharts frontend"
      status: resolved
      verification: explicit
  artifacts:
    - path: src/services/runtimeMode.ts
      provides: "One immutable startup mode consumed by all runtime boundaries"
    - path: src/services/api.ts
      provides: "KB-denied unsupported/trading facade without demo module evaluation"
    - path: src/pages/TradingPage.tsx
      provides: "Original native shell with KB read-only/unavailable panel states"
    - path: src/__tests__/native-bootstrap.test.tsx
      provides: "Native startup, import/network isolation and demo compatibility checks"
  key_links:
    - from: src/services/runtimeMode.ts
      to: src/main.tsx
      via: "Startup-fixed mode before MarketDataBridge mount"
    - from: src/services/runtimeMode.ts
      to: src/services/api.ts
      via: "Mode chosen before demoApi/engine evaluation"
    - from: src/services/runtimeMode.ts
      to: src/services/ws.ts
      via: "Inactive KB facade before demo/feed import/start"
    - from: src/services/runtimeMode.ts
      to: src/lib/posthog.ts
      via: "Initialization and capture gate before page imports execute"
    - from: src/App.tsx
      to: src/pages/TradingPage.tsx
      via: "Original component chain with only demo bootstrap conditional"
---

<objective>
D-05 사용자 정정대로 원래 OpenCharts frontend를 KB 모드로 안전하게 시작한다. 실제 App → TradingPage 경로를 유지하고 공유 서비스와 UI에서 데모·거래·자동 조회를 막는다. 기존 01-01 backend는 완료 상태로 재사용하며, native quote/chart 입력 연결은 01-05가 이어서 완성한다.
</objective>

<execution_context>
executing-plans subagents mode + subagent-driven-development + TDD. Sole implementation/fix worker: gpt-5.6-sol; independent spec then quality reviewers: gpt-6-astra. No model substitution or gsd-execute-phase. One coherent runtime task uses the bounded chunks below; review once after the whole task, fix/re-review before 01-05. Preserve other agents' edits and root-owned STATE/01-EXECUTION.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-us-kr-poc/01-CONTEXT.md
@.planning/phases/01-us-kr-poc/01-SPEC.md
@.planning/phases/01-us-kr-poc/01-UI-SPEC.md
@.planning/phases/01-us-kr-poc/01-01-SUMMARY.md
@.planning/phases/01-us-kr-poc/SOURCE-AUDIT.md
</context>

<repository_map>
All files and commands belong to the assigned OpenCharts isolated worktree. OC and KB names in cwd metadata mean their orchestrator-assigned workdirs, not nested paths. Publish configuration names only; do not copy private local values. No new dependency, backend gate, schema or live read is required here.
</repository_map>

<runtime_ownership>
Before native implementation, root gracefully stops only its own prototype frontend, preserves the accepted running KB backend and unrelated original apps, and coordinates restart of the native frontend after final browser verification. Serialize Vite servers and builds that share the OC worktree/cache; do not introduce extra cache configuration without an actual need. Implementation and fixture checks make no live provider reads. Reuse accepted backend evidence; only 01-07 may perform the needed approved bounded final normal native US/KR observations with the existing owner.
</runtime_ownership>

<interfaces>
NEW src/services/runtimeMode.ts exports a single immutable application mode and isKbMode boolean. Resolve the already-used Vite startup mode kb to KB; existing development/production/test/default startup remains demo. Unknown explicit startup modes fail closed rather than silently enabling demo trading. User input, query parameters and storage cannot switch it after module evaluation. Do not introduce a new configurable URL or runtime mode switch.
Existing api and ws public signatures remain compatible in demo mode. In KB mode unsupported legacy data/mutation methods reject a fixed sanitized unsupported/read-only error, never return a success-shaped null or forward requests. The approved marketSnapshot.ts client remains the only snapshot/account source.
</interfaces>

## Source-grounded seams

| File:span | Required implication |
|---|---|
| App.tsx:18–33; main.tsx:71 | Gate demoLogin/is_demo/loadSymbols/loadAccounts and MarketDataBridge, still render TradingPage |
| services/api.ts:10,26–31; demo/api.ts:4 | Static demo import evaluates engine; isolate evaluation before KB facade creation and remove KB null-success fallback |
| services/ws.ts:11,29–34 | KB must never import/start demo feed or subscribe to WS |
| lib/posthog.ts:7–13,23–29; TradingPage.tsx:82–87 | Guard module initialization and all capture paths, even with a configured test key |
| store.tsx:277–315,327–359,473–478 | Keep KB source identity out of trading accounts, persisted active_account and tick stores; no stale demo user hydration |
| queries.ts:106–133,257–301 | Disable symbols, tick/candle, positions/orders/metrics/health polling and retry/refocus work in KB |
| TradingPage.tsx:288–309,518–660 | Guard context/mobile/order/AI calls; render native panel shells with unavailable/read-only content |
| DOMPanel.tsx:21–40; ChartPanel.tsx:230,252; ChartContextMenu.tsx:105 | Do not mount randomized depth in KB; omit only order callbacks, not local drawing interactions |

## Artifacts this plan produces

runtimeMode.ts and its startup-mode exports; modified original App/main/api/ws/store/queries/PostHog/TradingPage boundaries; runtime-mode.test.ts and native-bootstrap.test.tsx. No substitute page or new chart is produced.

## Bounded execution chunks

| Order | Active files (maximum 5) | Focused RED/GREEN |
|---|---|---|
| A | runtimeMode.ts, api.ts, ws.ts, posthog.ts, runtime-mode.test.ts | `npm test -- src/__tests__/runtime-mode.test.ts`: mode-fixed module evaluation, feed/telemetry and shared API-denial cases |
| B | main.tsx, App.tsx, store.tsx, native-bootstrap.test.tsx | `npm test -- src/__tests__/native-bootstrap.test.tsx`: native shell/KB bootstrap with persisted demo sentinels and demo startup control |
| C | queries.ts, TradingPage.tsx, runtime-mode.test.ts, native-bootstrap.test.tsx | Both focused files: zero unsupported queries/polls and all UI/service trading entrances denied |

Same worker carries current SHA, changed/uncommitted files, exposed signatures, command/result and unresolved contract notes in a compact progress message. Reopen only an affected earlier file, never the entire groups. Chunks are not extra tasks/review cycles. Whole-task spec then quality review precedes dependent work.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: 원래 App/TradingPage를 고정 KB 모드의 읽기 전용 shell로 실행한다</name>
  <files>src/services/runtimeMode.ts, src/main.tsx, src/App.tsx, src/services/api.ts, src/services/ws.ts, src/services/store.tsx, src/services/queries.ts, src/lib/posthog.ts, src/pages/TradingPage.tsx, src/__tests__/runtime-mode.test.ts, src/__tests__/native-bootstrap.test.tsx</files>
  <read_first>Current changed files once at the source spans above; both AGENTS; corrected CONTEXT/SPEC/UI-SPEC; existing marketSnapshot.ts public exports and current Vite kb mode. Existing useChartPreferences/drawingStyles call browser-local helpers; chartTemplatesApi is a separate direct local API, not a broker write.</read_first>
  <behavior>
    - A test mounts the real App→TradingPage chain in KB mode and observes zero demo login, engine evaluation, demo feed, WS, telemetry initialization/capture and legacy data requests.
    - Persisted demo account/user/position/tick sentinels are neither hydrated nor displayed in KB; stored demo values are not deleted and still work in demo mode.
    - Every exposed legacy trading/account-mutation method rejects before demo/HTTP work, including direct calls bypassing hidden buttons; unsupported reads are explicit failures.
    - KB native order/context/mobile/AI and fake-depth/portfolio surfaces cannot trigger writes or synthesize data. Local templates and preference helpers remain usable.
    - Demo startup control still loads its native symbols/accounts/feed and existing original page.
  </behavior>
  <action>
    Implement D-03/D-05/D-06/E-07/E-08 with failing module-boundary and real native bootstrap tests first. Use one startup mode source, not unrelated isDemo/store flags. Make demo API/feed loading conditional before their engine module is evaluated; preserve their existing signatures and connect/disconnect lifecycle in demo, including a late dynamic load after disconnect/unmount if lazy imports are used. Do not reroute generic api methods to KB.

    Skip App's demo login/is_demo storage mutation and account/symbol boot in KB, but render the same TradingPage. Main does not mount MarketDataBridge in KB. PostHog initialization and capturePlatform/page capture all respect the startup guard even when a test key is set. KB auth/trading store initial state stays empty rather than hydrating persisted demo state; preserve stored demo values. Do not invent Account/User records for KB.

    Disable every legacy automatic read path in queries and TradingPage: symbol list, api.getTick effect, useCandles, deep-fetch timer, position/order/metrics/health polling, retries and focus refetch. Hooks stay unconditional according to React rules; their enabled/effect guards enforce KB inactivity. 01-05 will supply native data directly from the reviewed manual client.

    Block trading mutations at the shared facade and at native context/mobile/order controls. Within the existing TradingPage layout render explicit unavailable/read-only content instead of mounting random DOM depth, fake portfolio/account metrics, order entry or unsupported AI activity. Omit ChartPanel onQuickOrder/onModifyPosition in KB; keep drawing/prefs/template callbacks distinct. Do not blanket-disable native drawing tools or browser-local chartTemplatesApi/readTraderPrefs/writeTraderPrefs. Their full KB drawing persistence is completed in 01-05. The unsupported write facade must not intercept these separate local helpers.

    Run the complete task checks and independent spec then quality review per D-07. P-01/P-02 cases assert direct service calls and actual native imports; P-04 asserts the original App/TradingPage chain, not just a label. No accepted backend behavior or live gate changes are authorized.
  </action>
  <verify>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm test -- src/__tests__/runtime-mode.test.ts src/__tests__/native-bootstrap.test.tsx</automated>
    <fails_when>Vitest exits nonzero, collects no tests, or detects demo module/feed/telemetry work, forbidden queries/mutations, fake panel data, stale demo state or broken demo startup.</fails_when>
  </verify>
  <acceptance_criteria>Original native shell starts safely in fixed KB mode; every direct trading mutation and automatic legacy-data route is blocked; demo behavior and pure local preferences/templates remain. No native quote/chart completion is claimed before 01-05. Both task reviews pass.</acceptance_criteria>
  <done>A user can open the original OpenCharts shell in KB read-only mode without starting demo/account/trading activity, and demo mode remains usable.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Startup mode → module initialization | Decide before demo engine/feed/telemetry evaluation |
| Native UI → shared API | UI hiding alone cannot authorize direct order mutations |
| Persisted demo state → KB display | Account identity and data cannot cross modes |
| Local preferences/templates → browser storage | Local chart interactivity is permitted; broker mutations are separate |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Elevation of Privilege | api/ws facade | high | mitigate | Direct method denial tests before module/transport effects |
| T-04-02 | Information Disclosure | PostHog/bootstrap/store | high | mitigate | Configured-key init/capture spy and stale persisted identity sentinels |
| T-04-03 | Spoofing | native panels | high | mitigate | No random depth or inherited demo portfolio; unavailable native state |
| T-04-04 | Denial of Service | hooks/timers | medium | mitigate | All KB automatic legacy reads disabled and fake-timer/network assertions |
| T-04-SC | Tampering | dependencies | low | accept | Existing installed locked dependencies only |
</threat_model>

<verification>
New tests are created before RED and must run actual relevant imports/component paths. Prohibition evidence is runtime-mode/native-bootstrap checks plus reviewed backend/proxy evidence; P-03 remains enforced by the unchanged client/backend and is exercised again in 01-05/06. Historical standalone tests/reviews do not prove native isolation. The historical 503 cause remains unconfirmed.
</verification>

<success_criteria>
SAFE-01/SAFE-02 runtime boundaries are independently reviewed and ready for 01-05. No backend/API/schema regression or new runtime dependency is introduced.
</success_criteria>

<output>
Create .planning/phases/01-us-kr-poc/01-04-SUMMARY.md with exact SHA, chunk/task evidence, module/API boundary decisions and independent reviews. Do not edit root-owned STATE/01-EXECUTION, push, open PRs or merge in this plan.
</output>
