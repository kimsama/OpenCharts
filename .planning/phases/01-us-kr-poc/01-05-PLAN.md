---
phase: 01-us-kr-poc
plan: "05"
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/pages/TradingPage.tsx
  - src/pages/trading/ChartToolbar.tsx
  - src/pages/trading/ChartPanel.tsx
  - src/pages/trading/utils.ts
  - src/pages/trading/useIndicators.ts
  - src/lib/indicators.ts
  - src/hooks/useChartDrawings.ts
  - src/services/marketSnapshotDrawings.ts
  - src/lib/chart-plugins/helpers/time.ts
  - src/lib/chart-plugins/tooltip/tooltip.ts
  - src/lib/chart-plugins/delta-tooltip/delta-tooltip.ts
  - src/__tests__/native-market.test.tsx
  - src/__tests__/native-chart.test.tsx
autonomous: true
requirements: [QUERY-01, QUERY-02, QUERY-03, QUERY-04, DATA-03, SAFE-01, SAFE-02, VERIFY-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/UI-01: Existing native symbol controls accept safe account, US/KR, query market and arbitrary ticker; explicit button/Enter alone fetches snapshot."
    - "E-02/UI-02: Full submitted identity owns results/drawings; editing does not relabel old data and late requests cannot cross account/market/query-market boundaries."
    - "D-04/E-03/E-06/UI-03: Native quote/legend shows actual currency/source/time/delay and nullable quote/volume; malformed data is rejected by the reused client."
    - "D-05/UI-06/UI-07: Original ChartPanel, supported indicators, drawing add/edit/delete/undo/redo, local templates and layout/preferences remain usable."
    - "E-07/E-08: KB native daily chart cannot request history, stale/gap/focus/live updates or synthesize ticks."
    - "E-09/UI-08: Original trading dates survive the numeric chart/drawing bridge and actual axis/crosshair/tooltip formatters in Seoul and US zones; the coordinate is never provider-as-of."
    - "E-10/E-11: Drawings persist only in their full identity scope; absent volume produces no histogram/legend zero or manufactured VWAP, while reported zero remains valid."
  prohibitions:
    - statement: "P-01: No order TR, arbitrary provider path, credential leak, raw payload in public response"
      status: resolved
      verification: explicit
    - statement: "P-02: No synthetic quote/volume, demo portfolio/depth or crypto replay fallback in KB mode"
      status: resolved
      verification: explicit
    - statement: "P-03: No relabelling an unverified Cboe instrument as Nasdaq"
      status: resolved
      verification: explicit
    - statement: "P-04: No separate connected HTML/page/chart product replacing the original OpenCharts frontend"
      status: resolved
      verification: explicit
  artifacts:
    - path: src/pages/trading/ChartToolbar.tsx
      provides: "Manual account/market/query-market/ticker controls inside existing symbol surface"
    - path: src/pages/TradingPage.tsx
      provides: "Native manual lookup ownership and safe account source separate from demo trading store"
    - path: src/pages/trading/ChartPanel.tsx
      provides: "Manual-daily native chart, nullable quote/volume legend and lifecycle guards"
    - path: src/services/marketSnapshotDrawings.ts
      provides: "Small scoped browser-local drawing persistence, no demo engine or network"
    - path: src/lib/chart-plugins/helpers/time.ts
      provides: "Explicit UTC trading-date formatting used by native/plugin daily surfaces"
    - path: src/__tests__/native-chart.test.tsx
      provides: "Date/timezone/null/indicator/drawing and lifecycle regression checks"
  key_links:
    - from: src/pages/trading/ChartToolbar.tsx
      to: src/pages/TradingPage.tsx
      via: "Explicit full-identity submission, not symbol-selection auto fetch"
    - from: src/pages/TradingPage.tsx
      to: src/services/marketSnapshot.ts
      via: "Existing fetchMarketAccounts/fetchMarketSnapshot/validateMarketSnapshot and abort plus ownership guard"
    - from: src/pages/TradingPage.tsx
      to: src/pages/trading/ChartPanel.tsx
      via: "Manual-daily mode, full identity, normalized bars/quote/provenance and loading/error state"
    - from: src/hooks/useChartDrawings.ts
      to: src/services/marketSnapshotDrawings.ts
      via: "Explicit KB scope and same existing state/history operations"
    - from: src/pages/trading/ChartPanel.tsx
      to: src/pages/trading/useIndicators.ts
      via: "Reported nullable-volume candle input; strict KB VWAP policy"
    - from: src/pages/trading/ChartPanel.tsx
      to: src/lib/chart-plugins/helpers/time.ts
      via: "Manual-daily axis/crosshair and tooltip/delta-tooltip formatter injection"
---

<objective>
원래 TradingPage/ChartToolbar/ChartPanel에 검토된 KB snapshot을 공급한다. D-05의 기존 layout·지표·드로잉·로컬 설정을 유지하면서 D-01..D-04의 양 시장 수동 조회, nullable volume, 거래일, 요청 소유권을 구현한다. Backend/client/proxy를 재작성하지 않는다.
</objective>

<execution_context>
executing-plans subagents + SDD/TDD; sole gpt-5.6-sol worker; independent gpt-6-astra spec then quality after the whole coherent native task. Use bounded groups below rather than holding all 13 files active. Resolve findings before 01-06. Preserve other work and root-owned STATE/01-EXECUTION; no model substitution.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-us-kr-poc/01-CONTEXT.md
@.planning/phases/01-us-kr-poc/01-SPEC.md
@.planning/phases/01-us-kr-poc/01-UI-SPEC.md
@.planning/phases/01-us-kr-poc/01-01-SUMMARY.md
@.planning/phases/01-us-kr-poc/01-04-SUMMARY.md
@src/services/marketSnapshot.ts
</context>

<repository_map>
All changed files and commands belong to assigned OC. Reuse approved KB HTTP/DTO unchanged. Runtime configuration is names-only in public artifacts; safe account aliases and opaque IDs remain local UI state, never telemetry. Existing user runtime and accepted provider ownership stay untouched.
</repository_map>

<runtime_ownership>
Before native implementation, root gracefully stops only its own prototype frontend, preserves the accepted running KB backend and unrelated original apps, and coordinates restart of the native frontend after final browser verification. Serialize Vite servers and builds that share the OC worktree/cache; do not introduce extra cache configuration without an actual need. Implementation and fixture checks make no live provider reads. Reuse accepted backend evidence; only 01-07 may perform the needed approved bounded final normal native US/KR observations with the existing owner.
</runtime_ownership>

## Source-grounded interfaces

| Existing source | Contract to preserve or extend |
|---|---|
| marketSnapshot.ts:86–145 | Existing MarketSnapshotIdentity/MarketSnapshot, safe accounts, fetch and strict DTO validation |
| TradingPage.tsx:221–246,319–400,420–500 | Legacy automatic symbols/ticks/candles are guarded by 01-04; pass manual data through original toolbar/chart props |
| ChartToolbar.tsx:142–149,160–265,290–309 | Existing symbol selector is list-only; extend it for explicit arbitrary input and safe account/query market; daily-only KB interval |
| ChartPanel.tsx:132–190,1612–1627,1789–1878 | Disable history fetch, stale/watchdog/gap work and tick/live smoothing in manual-daily |
| ChartPanel.tsx:323–325,600–607,888–966,1548–1558,2082–2136 | Private ChartLegendHeader/OhlcvLegendRow/BidAskRow and candle mapping currently require/fill volume and quote sides; preserve missing values |
| trading/utils.ts:50–58; useIndicators.ts:187–196; lib/indicators.ts:246–258 | Current conversion injects zero volume and vwap substitutes one; KB must consume reported volume under strict policy |
| ChartPanel.tsx:1440–1495,1728–1740 | No existing explicit time formatter: add manual-daily axis/crosshair UTC date formatting |
| chart-plugins/helpers/time.ts:3–33; tooltip/tooltip.ts:205–230; delta-tooltip/delta-tooltip.ts:201–271 | Plugins use browser-local formattedDateAndTime; inject date-only formatting for manual-daily without changing demo default |
| useChartDrawings.ts:20–158 | list/save/remove/clear and undo/redo can retain state/history while selecting an explicit KB local persistence path |
| demo/api.ts:29,42–60 | Symbol-only key and DEMO_SYMBOLS-only remove are not safe KB persistence |
| useChartPreferences.ts:178–181,221–234; useTraderPreferences.ts:24–32; drawingStyles.ts:31–83 | Pure browser-local preference helpers already exist; preserve them without fake user or remote hook |
| chart-templates.ts:33–55; ChartTemplatesMenu.tsx:55–76 | Direct chartTemplatesApi is local CRUD, independent of blocked broker writes; retain it |

<interfaces>
Add optional manual-daily/dataIdentityKey and normalized snapshot quote/provenance inputs to original ChartPanel; default demo behavior stays unchanged. Extend existing toolbar props with a KB-only manual-control contract. Keep the full KB identity in TradingPage state rather than Account/TickEntry/demo selected-account stores.

Extend trading/utils.ts with the small pure snapshot-to-native-row conversion and Decimal-scale precision helper. The DTO date remains a string; the numeric chart coordinate is Date.UTC(year, month-1, day)/1000, used only for native drawing/chart math. Original date strings remain associated with normalized rows/provenance. Manual rows allow volume number or null without globally changing the demo Candle schema.

Add a reported-volume input/policy to useIndicators and an optional requireReportedVolume=false parameter to existing vwap. KB passes true: any missing/nonfinite/negative volume makes VWAP unavailable; real zero contributes zero and no point is emitted while cumulative volume is zero. Demo default behavior remains unchanged. Price-only indicators consume the same OHLC regardless of volume.

Add a small optional trading-date formatter to existing tooltip/delta-tooltip option contracts, supplied only in manual-daily; defaults retain existing demo formatting. Extend helpers/time.ts with explicit UTC date-only formatting rather than relying on its browser-local conversion.

marketSnapshotDrawings.ts exports scoped local list/save/remove/clear operations using a serialized tuple ['kb', account_id, market, query_market, symbol]. useChartDrawings accepts this optional scope, retains the same drawing/history interface and selects that local path only in KB. No engine import, provider request or generic persistence framework.
</interfaces>

## Artifacts this plan produces

Modified original TradingPage, ChartToolbar, ChartPanel (including its private legend), utils/useIndicators/vwap, useChartDrawings and two existing tooltip plugins; new small marketSnapshotDrawings.ts and native-market/native-chart behavioral tests. No new screen, chart component, account model, backend API or runtime dependency.

## Bounded execution chunks

| Order | Active group (maximum 5 files) | Focused RED/GREEN |
|---|---|---|
| A | trading/utils.ts, ChartPanel.tsx relevant data/legend spans, useIndicators.ts, lib/indicators.ts, native-chart.test.tsx | `npm test -- src/__tests__/native-chart.test.tsx`: explicit date mapping, missing/reported-zero volume, quote precision and strict VWAP |
| B | TradingPage.tsx, ChartToolbar.tsx, native-market.test.tsx | `npm test -- src/__tests__/native-market.test.tsx`: real native control→client→chart prop path with controlled HTTP transport, both markets and full-tuple races |
| C | useChartDrawings.ts, marketSnapshotDrawings.ts, native-chart.test.tsx | Focused native-chart suite: local CRUD, undo/redo, reload and scope transitions |
| D | helpers/time.ts, tooltip.ts, delta-tooltip.ts, ChartPanel.tsx formatter spans, native-chart.test.tsx | Focused native-chart suite: axis/crosshair and both plugin formatter hooks in Seoul/US, lifecycle/history/live suppression |

Carry current SHA, changed/uncommitted files, DTO/signature summaries, RED/GREEN result and unresolved issues in a compact worker message after each chunk. Reuse read source spans/context; no whole-file reloading of the large ChartPanel. All chunks remain one uncompleted task until full verification and independent spec→quality review pass.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Native 종목 입력→KB snapshot→원래 chart·legend·drawing 경로를 완성한다</name>
  <files>src/pages/TradingPage.tsx, src/pages/trading/ChartToolbar.tsx, src/pages/trading/ChartPanel.tsx, src/pages/trading/utils.ts, src/pages/trading/useIndicators.ts, src/lib/indicators.ts, src/hooks/useChartDrawings.ts, src/services/marketSnapshotDrawings.ts, src/lib/chart-plugins/helpers/time.ts, src/lib/chart-plugins/tooltip/tooltip.ts, src/lib/chart-plugins/delta-tooltip/delta-tooltip.ts, src/__tests__/native-market.test.tsx, src/__tests__/native-chart.test.tsx</files>
  <read_first>Corrected CONTEXT/SPEC/UI-SPEC, reviewed 01-04, source spans above and the current files' relevant signatures; existing marketSnapshot client/proxy contracts are already reviewed and remain reused. Do not copy or preserve MarketLookupPage/MarketSnapshotChart as the product UI.</read_first>
  <behavior>
    - Native toolbar accepts arbitrary normalized US symbols and exact six-digit KR strings, safe account and explicit routing; typing/selecting alone performs zero snapshots, button/Enter performs one.
    - Native TradingPage uses the existing client and commits only the full latest submitted identity. Old aborted completion, account/market switch, unmount, empty/error/malformed results clear incompatible data and drawings without relabeling a prior submitted result.
    - Original ChartPanel renders real quote/candles with USD/KRW, Decimal precision, source/acquisition/provider time/delay and nullable sides/volume. It never creates a fake TickEntry.
    - Manual daily mode disables scroll/history, deep fetch, stale/watchdog/gap/live updates and unsupported intervals; local zoom, drawings, indicators and settings still operate.
    - Native numeric coordinates round-trip original dates; axis/crosshair and both tooltip plugins display that original day under Seoul/New York and DST cases. Provider-as-of remains nullable and distinct.
    - Reported zero volume is not missing; missing histogram/legend rows remain unavailable, strict VWAP returns no fabricated line, price indicators still work.
    - KB drawing add/edit/delete/clear/undo/redo/reload uses only the current tuple scope; transition resets drawing state/history and never deletes another scope or demo drawing.
  </behavior>
  <action>
    Implement D-01/D-02 in the existing symbol popup and native toolbar, retaining its layout/styles and accessible labels. Add safe account selection there using fetchMarketAccounts; do not coerce safe accounts into trading Account objects. The selector's old list filter is not arbitrary ticker support. Daily mode is explicit; other intervals are disabled in KB with truthful unavailable text. TradingPage owns account/market/query-market/symbol draft and separately immutable submitted identity. Use the reviewed client, AbortController plus monotonic commit guard; edits leave old submitted identity unchanged until a new request, and identity-invalidating changes clear incompatible results.

    Feed normalized manual rows and quote/provenance directly into original ChartPanel/its private ChartLegendHeader. Keep tick/liveCandle undefined in KB and do not route quotes into demo store TickEntry. At the chart boundary validate finite Number conversions and use decimal-string scales for price precision rather than the demo default five digits. Preserve date strings and nullable volume. Clear candle, volume and legend together on invalidation/loading/error/empty, including the current empty-array early return; dispose observer/chart/plugin lifecycle as before.

    Add manual-daily behavior guards at every mapped history/stale/watchdog/gap/smoothing path. Retain normal chart zoom/pan and local drawing math. For dates use numeric UTC-midnight strictly as a calendar coordinate, and supply explicit UTC date-only axis/crosshair and tooltip/delta-tooltip formatters. The browser timezone and local formatting controls must not shift the displayed trading day. Never show this coordinate as provider time. Keep existing demo formatting and unrelated BusinessDay parsing outside this change; test the actual plugin option wiring, not a helper alone.

    Preserve original indicator controls and price-only calculations. Pass reported nullable volume into indicator inputs instead of toIndicatorCandles' fabricated zero path in KB. Extend vwap with the explicit strict parameter described above; missing input yields unavailable output and an explained native status, real zero retains its meaning. Keep the demo default unchanged. No bar/quote/depth/portfolio fill values are added to satisfy old non-null types.

    Preserve useChartDrawings state/history and implement its small explicit KB local persistence path. Scope list/save/remove/clear to the complete tuple, reset loaded drawings and undo/redo on identity change, and guard late load results. Same-scope reload persists drawings; different accounts/query markets never share them. Do not use the demo symbol enumerator for delete or a global ID scan. Local preference helpers, chartTemplatesApi, indicator selection, layout height and drawing styles remain existing browser-local behavior; no fake auth user or remote preference hook is added. Trading callbacks remain omitted independently of these local operations.

    Run both new native test suites with service/proxy regressions, then independent spec and quality review per D-07. 01-06 will replace the standalone browser suite and verify the real HTTP path, actual timezone/canvas interactions and single product entry; do not call unit/transport-fixture evidence a completed browser/live pass.
  </action>
  <verify>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm test -- src/__tests__/native-market.test.tsx src/__tests__/native-chart.test.tsx src/__tests__/market-snapshot.test.ts src/__tests__/market-proxy.test.ts</automated>
    <fails_when>Vitest exits nonzero, collects no required tests, or native submission/ownership/date/plugin/volume/VWAP/drawing/local-preference/readonly assertions fail. Missing required timezone/plugin cases leave the task incomplete.</fails_when>
  </verify>
  <acceptance_criteria>Both markets use original toolbar/page/chart and reviewed client; all E-01..E-11/P-01..P-04/UI-01..UI-08 implementation behaviors have focused evidence. Full-identity drawing persistence and native date/plugin formatting are explicit. Both reviews pass; actual browser proof remains mandatory in 01-06.</acceptance_criteria>
  <done>A user can manually inspect either market in the original frontend, with accurate missing/date/provenance semantics and working local chart tools, without any trading or automatic provider activity.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Native draft → submitted response | Full tuple and request ownership prevent mixed data |
| Normalized DTO → numeric chart/indicators | Finite precision, calendar coordinate and nullable-volume semantics |
| Local drawing → persisted identity scope | Account/query market isolation without broker access |
| Native callbacks → service/local helpers | Broker writes denied, local annotation/settings allowed |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Spoofing | request/response state | high | mitigate | Full tuple validation and late-response/identity reset tests |
| T-05-02 | Tampering | time/volume/indicators | high | mitigate | UTC-date plugin/axis tests; strict reported-volume VWAP; no null-to-zero coercion |
| T-05-03 | Information Disclosure | drawing storage | medium | mitigate | Tuple-scoped local CRUD; no telemetry/account identifiers in public artifacts |
| T-05-04 | Elevation of Privilege | native callbacks | high | mitigate | Trading callbacks absent and service denied, local chart helpers explicitly retained |
| T-05-05 | Denial of Service | chart data acquisition | medium | mitigate | Disable every history/stale/gap/live path; timer/call-count tests |
</threat_model>

<verification>
P-01 is inherited from reviewed API/proxy/shared guard and exercised by direct native actions; P-02 by native null/volume/panel cases; P-03 by existing DTO validation and query-routing labels; P-04 by actual original component assertions. Date-roundtrip tests alone are insufficient: actual formatter injection and 01-06 browser timezone evidence are required. Backend 01-01 remains complete; historical 503 cause remains unconfirmed.
</verification>

<success_criteria>
Complete corrected native data/chart behavior with bounded file groups and reviewed tests. No new screen, auth identity, runtime package or backend operation. Existing local tools/preferences remain functional.
</success_criteria>

<output>
Create .planning/phases/01-us-kr-poc/01-05-SUMMARY.md with actual commit/test/review evidence, chart/date/volume/drawing contracts and remaining 01-06 browser work. No root STATE/01-EXECUTION edits, push/PR/merge.
</output>
