---
phase: 01-us-kr-poc
plan: "06"
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - index.html
  - vite.config.ts
  - e2e/market-lookup.spec.ts
  - playwright.kb.config.ts
files_deleted:
  - kb.html
  - src/kb-main.tsx
  - src/pages/MarketLookupPage.tsx
  - src/components/MarketSnapshotChart.tsx
  - src/__tests__/market-lookup.test.tsx
autonomous: true
requirements: [QUERY-01, QUERY-02, QUERY-03, QUERY-04, DATA-03, SAFE-01, SAFE-02, VERIFY-01, VERIFY-02]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-05/P-04/UI-06: One index entry renders the original App/TradingPage/ChartPanel; standalone product files and build entry are removed."
    - "D-01/D-02: Actual browser submission for US/KOSPI/KOSDAQ reaches real proxy/backend/coordinator/adapter fixtures and renders native quote/candles."
    - "D-03/E-07/E-08/UI-07: Browser sees zero demo/feed/telemetry/trading/depth/portfolio/polling/history requests in KB mode, including after native controls and timers are exercised."
    - "E-02/E-05/E-06: Account/market changes, late completion, provider/offline/empty/malformed cases never commit stale native data."
    - "E-09/E-10/E-11/UI-08: Native date labels remain original in Seoul/US zones; missing volume stays unavailable; real drawing/indicator/local-preference interactions remain usable."
    - "UI-01/UI-04: Original desktop layout and 390px responsive controls are keyboard accessible with announced errors."
  prohibitions:
    - statement: "P-01: No order TR, arbitrary provider path, credential leak, raw payload in public response"
      status: resolved
      verification: explicit
    - statement: "P-02: No synthetic quote/volume, demo portfolio/depth or crypto replay fallback in KB mode"
      status: resolved
      verification: explicit
    - statement: "P-03: No relabelling an unverified Cboe instrument as Nasdaq"
      status: resolved
      verification: explicit
    - statement: "P-04: No separate connected HTML/page/chart product replacing the original OpenCharts frontend"
      status: resolved
      verification: explicit
  artifacts:
    - path: index.html
      provides: "Only product HTML entry for fixed startup modes"
    - path: vite.config.ts
      provides: "Single-entry builds and unchanged exact KB proxy"
    - path: e2e/market-lookup.spec.ts
      provides: "Native UI real HTTP regression suite replacing standalone assertions"
    - path: playwright.kb.config.ts
      provides: "Owned loopback fixture/backend and native index startup"
  key_links:
    - from: index.html
      to: src/main.tsx
      via: "Original entry in both startup modes"
    - from: e2e/market-lookup.spec.ts
      to: src/pages/trading/ChartPanel.tsx
      via: "Actual native controls/canvas/legend/drawing interaction"
    - from: playwright.kb.config.ts
      to: kb-us-stock-trading-journal/tests/market_snapshot_fixture_server.py
      via: "Reuse actual isolated FastAPI provider-transport fixture; no normalized response interception"
---

<objective>
D-05 정정에 따라 별도 product를 제거하고 실제 브라우저에서 네이티브 US/KR 경로를 입증한다. 기존 client/proxy/fixture와 완료된 backend를 재사용하며 새 runtime dependency나 provider operation을 추가하지 않는다.
</objective>

<execution_context>
executing-plans subagents + SDD/TDD; sole Sol worker, independent Astra spec then quality review once after this task. Respect 01-04/05 reviewed contracts and ownership. No gsd-execute-phase, new review cycles per chunk, or model substitution.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-us-kr-poc/01-CONTEXT.md
@.planning/phases/01-us-kr-poc/01-SPEC.md
@.planning/phases/01-us-kr-poc/01-UI-SPEC.md
@.planning/phases/01-us-kr-poc/01-04-SUMMARY.md
@.planning/phases/01-us-kr-poc/01-05-SUMMARY.md
@.planning/phases/01-us-kr-poc/01-02-SUMMARY.md
</context>

<repository_map>
All changed/deleted files belong to assigned OC. The unchanged KB fixture backend belongs to assigned KB; KB_REPO_DIR is supplied locally by the orchestrator. KB_MARKET_BACKEND_PORT and PLAYWRIGHT_CHROME_PATH remain local configuration names; no machine/account coordinates appear in public evidence. One owner starts/stops only its fixture/Vite processes; preserve current user runtime and original dirty roots.
</repository_map>

<runtime_ownership>
Before native implementation, root gracefully stops only its own prototype frontend, preserves the accepted running KB backend and unrelated original apps, and coordinates restart of the native frontend after final browser verification. Serialize Vite servers and builds that share the OC worktree/cache; do not introduce extra cache configuration without an actual need. Implementation and fixture checks make no live provider reads. Reuse accepted backend evidence; only 01-07 may perform the needed approved bounded final normal native US/KR observations with the existing owner.
</runtime_ownership>

## Artifacts this plan produces

Updated single index/vite build, native e2e/market-lookup.spec.ts and playwright.kb.config.ts. Delete precisely the five standalone files in files_deleted; retain marketSnapshot.ts, market-snapshot.test.ts, market-proxy.test.ts and the backend fixture. Historical planning/review/validation remains available.

## Bounded execution chunks

| Order | Active group | Focused gate |
|---|---|---|
| A | e2e/market-lookup.spec.ts, playwright.kb.config.ts | Rewrite native route/control assertions first and observe intended RED; fixture servers remain actual application processes |
| B | index.html, vite.config.ts and the five deletion paths | Inspect references to the named obsolete exports only; remove files in two batches of at most four active paths, then run the native unit/proxy checks |
| C | e2e/market-lookup.spec.ts, playwright.kb.config.ts | Native real HTTP GREEN, then default and KB builds with the single-entry output check |

Same worker records SHA/changed files/RED-GREEN/remaining issues between chunks without checkpoint files. Do not reload reviewed 01-04/05 source wholesale. Return any discovered implementation correction to its explicit owner and rerun/review its affected behavior before final acceptance.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: 단일 원래 entry에서 실제 native US/KR 브라우저 경로를 검증한다</name>
  <files>index.html, vite.config.ts, e2e/market-lookup.spec.ts, playwright.kb.config.ts, kb.html, src/kb-main.tsx, src/pages/MarketLookupPage.tsx, src/components/MarketSnapshotChart.tsx, src/__tests__/market-lookup.test.tsx</files>
  <read_first>Current index/vite/Playwright config and existing E2E once; reviewed 01-04/05 summaries; existing unchanged service/proxy tests; fixture isolation findings in historical 01-02-REVIEW. Preserve fixture constructor-only settings/dotenv isolation. Do not read or print real backend configuration.</read_first>
  <behavior>
    - Test navigates / in fixed KB mode and finds the real native symbol controls/ChartPanel, not a standalone page. US/KOSPI/KOSDAQ submissions each produce one actual snapshot request and no legacy candle/tick requests.
    - Required E-01..E-11/P-01..P-04/UI-01..UI-08 cases use real component interactions and provider-transport fixtures; typing and all timer/focus/history paths cause zero automatic snapshot calls.
    - Both browser timezone contexts Asia/Seoul and America/New_York display the original candle trading dates on actual native crosshair/tooltip/time-axis surfaces, including dates around DST boundaries.
    - Native price indicator, draw/add/edit/delete/undo/redo, same-identity reload and different-account/query-market isolation work; chart preferences/template changes remain browser-local and produce no broker request.
    - Missing volume/quote sides/depth/portfolio stays unavailable, every order entrance is absent/disabled or denied, and configured telemetry key produces no init/capture traffic.
  </behavior>
  <action>
    Per D-01..D-06 replace standalone E2E expectations with actual native App/TradingPage controls and ChartPanel interactions. Keep the real Vite→FastAPI→coordinator→adapter path; only provider transport and credential storage are fixtures. Do not intercept the normalized snapshot response. Reuse existing isolated fixture server and scenarios without widening production backend or fixture access to real bindings.

    Remove the five exact standalone files and the index navigation link/multi-entry Vite input. Keep one index/src/main entry for default demo and mode kb. Preserve the existing narrow kbReadProxy deny/header/origin/redirect behavior and mode-specific loopback restriction. Re-run market-proxy tests for the changed Vite configuration. Rewrite the obsolete component test coverage into the already planned native tests before deleting it; all E/P/UI behaviors must still have executable owners.

    Update Playwright readiness and navigation to the native index. Retain one worker, no existing-server reuse, current safe fixture settings and a configured telemetry test key. Existing user runtime processes must not be reused or terminated. Keep provider/depth/trading/legacy-query network counters active while selecting market/account, editing and submitting, changing native local preferences, opening panels, invoking order shortcuts, scrolling left, focusing and waiting through former polling intervals. Use deterministic timer advancement where possible.

    Include real native desktop/390px interactions and crosshair/date assertions in Seoul and New York contexts. Exercise drawings and supported price indicators on actual canvas UI, plus an unavailable-volume scenario; unit date-roundtrip evidence alone is insufficient for native formatters. Check latest-result ownership, loading/error/empty clear, unsupported routing as query market only, and current quote precision. Keep secrets/private aliases/config values out of screenshots and traces shared as artifacts; fixtures use synthetic aliases.

    Run focused native/client/proxy unit checks, the real HTTP suite and each changed build mode once. Both build modes must produce only index.html. Original demo startup behavior is also covered by the 01-04 unit control; do not introduce broad terminal parity work. Run independent spec then quality review per D-07 and resolve findings before 01-07.
  </action>
  <verify>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm test -- src/__tests__/runtime-mode.test.ts src/__tests__/native-bootstrap.test.tsx src/__tests__/native-market.test.tsx src/__tests__/native-chart.test.tsx src/__tests__/market-snapshot.test.ts src/__tests__/market-proxy.test.ts</automated>
    <fails_when>Vitest exits nonzero or collects no required tests; native identity/date/null/drawing/runtime/proxy behavior regresses.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm exec -- playwright test --config playwright.kb.config.ts</automated>
    <fails_when>Playwright exits nonzero, required cases are skipped/not collected, fixture server fails, or actual US/KR/native date/drawing/read-only/mobile assertions fail.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm run build</automated>
    <fails_when>Vite build exits nonzero or the unchanged demo startup cannot build.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>npm run build -- --mode kb</automated>
    <fails_when>Vite KB build exits nonzero or its native entry imports fail.</fails_when>
    <cwd repository="OC">OpenCharts</cwd>
    <automated>node -e "const f=require('node:fs'); const h=f.readdirSync('dist').filter(x=>x.endsWith('.html')); if(h.length!==1||h[0]!=='index.html') throw new Error('Expected single native index entry')"</automated>
    <fails_when>Node exits nonzero because the final KB build has a missing or extra HTML entry.</fails_when>
    <human-check>Inspect the real native desktop and 390px browser once: recognizably original layout, original chart/controls/drawing/indicator surfaces, correct dates and missing values. Process startup or screenshot-only proof of an unrelated page is insufficient.</human-check>
  </verify>
  <acceptance_criteria>One final product entry; obsolete standalone files removed; all native E/P/UI cases covered and actual HTTP browser suite passes. Native date formatters, drawings and local preference actions are observed. Both independent reviews pass.</acceptance_criteria>
  <done>The native frontend goal is implemented and browser-verified; only final sanitized validation/PR delivery remains.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Native browser → proxy/backend fixture | Actual HTTP path with strict read allowlist |
| Test process → local environment | Keep constructor-only fixture settings and owned processes |
| Canvas interaction → request/storage | Local drawings/preferences must not trigger provider/trading operations |
| Verification output → public artifact | Synthetic aliases and configuration names only |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Information Disclosure | browser evidence/fixture | high | mitigate | Existing dotenv isolation, synthetic fixtures and artifact redaction |
| T-06-02 | Elevation of Privilege | proxy/runtime | high | mitigate | Actual forbidden-route/order/depth/telemetry request counters |
| T-06-03 | Spoofing | native date/identity UI | high | mitigate | Actual timezone/crosshair/identity assertions and missing-volume cases |
| T-06-04 | Repudiation | delivered UI | medium | mitigate | Original native component/browser evidence and single-entry artifact assertion |
</threat_model>

<verification>
P-01/P-02 are tested at runtime/service/proxy and real browser boundaries; P-03 by preserved normalized routing validation and native query-market label; P-04 by actual index/native component path and one-entry build. Existing accepted live provider evidence is reused separately; these fixture checks do not infer live entitlement or explain the historical 503.
</verification>

<success_criteria>
Corrected native frontend requirements are met without new backend scope or runtime dependencies. Old standalone approval is retained as historical evidence only.
</success_criteria>

<output>
Create .planning/phases/01-us-kr-poc/01-06-SUMMARY.md with exact OC/KB SHAs, native E/P/UI evidence, commands/build modes and reviews. Preserve root-owned STATE/01-EXECUTION. No push/PR/merge in this plan.
</output>
